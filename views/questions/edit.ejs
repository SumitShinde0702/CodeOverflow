<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Question - CodeOverflow</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container-fluid bg-dark text-white min-vh-100">
        <div class="container py-5">
            <h2><i class="fas fa-edit me-2"></i>Edit Question</h2>
            
            <form action="/questions/<%= question._id %>/edit" method="POST">
                <div class="mb-3">
                    <label for="title" class="form-label">Title</label>
                    <input type="text" class="form-control bg-secondary text-white" 
                           id="title" name="title" required
                           value="<%= question.title %>">
                </div>
                
                <div class="mb-3">
                    <label for="body" class="form-label">Body</label>
                    <textarea class="form-control bg-secondary text-white" 
                              id="body" name="body" rows="6" required><%= question.body %></textarea>
                </div>

                <div class="mb-3">
                    <label for="tags" class="form-label">Tags</label>
                    <input type="text" class="form-control bg-secondary text-white" 
                           id="tags" name="tags" 
                           value="<%= question.tags ? question.tags.join(', ') : '' %>"
                           placeholder="Add tags (comma separated)">
                </div>

                <div class="mt-4">
                    <a href="/questions" class="btn btn-secondary me-2">Cancel</a>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save me-2"></i>Save Changes
                    </button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 